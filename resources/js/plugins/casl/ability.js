import { createMongoAbility } from '@casl/ability'

// Solo crea y exporta el objeto 'ability' vacío inicialmente.
// Las reglas se añadirán después del login.
export const ability = createMongoAbility()

